TITLE PRINTSTR Program
PAGE 66,80 ; 66 lines per page, 80 characters/line

;; This is to understand the DOS print string function

CODESEG SEGMENT
	ASSUME CS:CODESEG,DS:CODESEG,ES:CODESEG,SS:CODESEG

ORG 100H
PROGRAM: ; NECESSARY OTHERWISE ORG 100 OFFSETS DON'T GET CALCULATED CORRECTLY
JMP		MAIN

MSG DB "HOLA MUNDO!",0DH,0AH,"$"

MAIN:
	MOV		AX,CS
	MOV		DS,AX ; SET DS TO CODESEG

	MOV		DX,OFFSET MSG
	MOV		AH,09H
	INT		21H

	MOV		AH,4CH
	MOV		AL,00H
	INT		21H
CODESEG ENDS
END PROGRAM
